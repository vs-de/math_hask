module Main where
import Prime

--main = mapM (putStrLn.show) $ map (\n -> fpf $ (big_prime 15)+n) [0..]
--main = putStrLn $ show $ is_prime fat_fat_prime2
--main = putStrLn.show $        
        --take 10 $ drop (10^6) primes'

--main = mapM (putStrLn.show) $
        --map (fpf.(+fat_fat_prime2)) [0..100]
--main = fac_demo_at (10^10000) 36

--main = putStrLn $ show $ 
  --fpfs 10111111111111111403333333333333333
--import MTGen
--main = write_files

--import StdLib
import System.IO
--main = hSetBuffering stdout NoBuffering >> (print $ keep_map (toInteger.p) $ takeWhile (<(2^24)) $ iterate (+(2^16)) (2^16))

--main = hSetBuffering stdout NoBuffering >> (print $ fpf 503468707164728717)

main = hSetBuffering stdout NoBuffering >>
      
      --print $ fpf 34567

